specVersion: "v1beta"
serviceId: "velora"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "velora-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
